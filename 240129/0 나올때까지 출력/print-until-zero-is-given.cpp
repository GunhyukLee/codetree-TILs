#include <iostream>
using namespace std;

int main() {
    int a;

    while (true) {
        cin >> a;
        if (a==0) {
            break;
        }
        cout << a << endl;
    }
    // 여기에 코드를 작성해주세요.
    return 0;
}